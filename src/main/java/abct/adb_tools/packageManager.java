package abct.adb_tools;

public class packageManager {
    // TODO:
    // - Clear package app data (cache + user)
    // String command = "adb -s " + id + " shell pm clear";

    // - uninstall app
    // String command = "adb -s " + id + " shell pm uninstall";

    // - get package list as HashMap<String>
    // adb shell pm list packages -3"|cut -f 2 -d ":
}
